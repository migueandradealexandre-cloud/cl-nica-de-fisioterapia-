[build]
  command = "npm run build"
  publish = "dist/client"

[functions]
  node_bundler = "esbuild"
  directory = "dist/server"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
